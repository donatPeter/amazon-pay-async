export enum OrderReferenceStates {
  Draft = 'Draft',
  Open = 'Open',
  Suspended = 'Suspended',
  Canceled = 'Canceled',
  Closed = 'Closed',
}

export enum AuthorizationStates {
  Pending = 'Pending',
  Open = 'Open',
  Declined = 'Declined',
  Closed = 'Closed',
}

export enum BillingAgreementStates {
  Draft = 'Draft',
  Open = 'Open',
  Suspended = 'Suspended',
  Canceled = 'Canceled',
  Closed = 'Closed',
}

export enum CaptureStates {
  Pending = 'Pending',
  Declined = 'Declined',
  Completed = 'Completed',
  Closed = 'Closed',
}

export enum RefurnStates {
  Pending = 'Pending',
  Declined = 'Declined',
  Completed = 'Completed',
}
